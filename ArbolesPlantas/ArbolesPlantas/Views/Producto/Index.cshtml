@model IEnumerable<ProyectoEcommerce.Models.productos>

@{
    ViewBag.Title = "Ordenes de compra";
}
<h1>Orden de compra </h1>
<h2>Cliente: </h2>
<p>

    
</p>

<table class="table">
    <tr>

        <th>
            COMPRAS
        </th>

        <th>
            Producto
        </th>

        <th>
            Precio
        </th>

        <th>
            TOTAL
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>


        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>

        <td>
           $ @Html.DisplayFor(modelItem => item.precio_venta_nuevo)
        </td>

        <td>
           $ @Html.DisplayFor(modelItem => item.precio_venta_nuevo)
        </td>




        <td>



            <a>
                <div style="margin-top: 45px;">
                    <button type="button" class="btn btn-success "> <font size="6" face="Poppins">@Html.ActionLink("Detalles", "Details", new { id = item.Id })</font></button>
                </div>
            </a>
            

        </td>
        
    </tr>
}

</table>
